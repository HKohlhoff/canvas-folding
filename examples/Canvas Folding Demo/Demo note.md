# Demo note

This local Markdown file is embedded in the Canvas Folding demo.

- Collapsing **Mixed root** hides this node and its incident edges.
- Focusing **Mixed root** keeps it active together with the group frame.
- Canvas Folding never changes this file or the surrounding `.canvas` file.
